# Environment variables for facility reservation API

# Database connection URL
# Development (default): postgres://postgres:postgres@localhost:5432/facility_reservation_db?sslmode=disable
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/facility_reservation_db?sslmode=disable

# Test database URL (used by make test-integration)
# TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5433/facility_reservation_db?sslmode=disable

# Application environment (affects logging format)
# ENV=development  # uses text logging (default)
# ENV=production   # uses JSON logging